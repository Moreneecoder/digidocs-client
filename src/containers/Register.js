const Register = () => (
  <div className="Register">
    {/* onSubmit={(e) => handleSubmit(e)} */}
    <form>
      <input className="form-control" required placeholder="Name" />
    </form>
  </div>
);

export default Register;
